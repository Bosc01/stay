# Pinned so a deploy installs the same versions that were tested.
# To upgrade: bump a pin, run `pytest`, and deploy deliberately.
fastapi==0.141.1
uvicorn[standard]==0.52.4
anthropic==1.4.0
supabase==2.31.0
python-dotenv==1.2.3
resend==2.43.0
pydantic==2.13.5
