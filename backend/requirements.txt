# Kept in sync with the root requirements.txt, which is the one CI installs.
# If you change a version here, change it there too.
fastapi==0.141.1
uvicorn[standard]==0.52.4
anthropic==1.4.0
supabase==2.31.0
python-dotenv==1.2.3
resend==2.43.0
pydantic==2.13.5
